### ALIASES ###

alias sdn='sudo shutdown -h now'

alias p='paru'

alias ls='exa'
alias la='exa -a --group-directories-first'
alias ll='exa -l --group-directories-first'
alias lla='exa -la --group-directories-first'
alias lm='exa -t -1'

alias cp='cp -iv'
alias mv='mv -iv'
alias rm='rm -v'
alias mkd='mkdir -pv'

alias zip='zip -r'
alias untar='tar -zxvf'

alias ds='du -csh'
alias dss='du -hs * | sort -h'
alias dts='dss; ds; df -h /'

alias myip='curl ipinfo.io/ip'

# Ultralist aliases
alias ull='ultralist list'
alias ula='ultralist add'
alias ulc='ultralist c'

# Code aliases
alias codium='codium .'
alias c='codium .'

# NVIM aliases
alias vim='nvim'
alias vi='nvim'
alias v='nvim'

# GIT aliases
alias g='git'
alias gua='git remote | xargs -L1 git push --all'

# Youtube-dl aliases
alias yt='youtube-dl --add-metadata -i'
alias yta='yt -x -f bestaudio/best'
alias ytc='youtube-dl -f "bestvideo[height>=720]+bestaudio/best" -ciw -o "%(title)s.%(ext)s" -v'

# Mpv aliases
alias play='mpv --fs "$1" & disown'

# Colorizing normal commands
alias grep='grep --color=auto'
alias diff='diff --color=auto'
alias ccat='highlight --out-format=ansi'
